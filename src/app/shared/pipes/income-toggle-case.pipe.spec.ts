import { IncomeToggleCasePipe } from './income-toggle-case.pipe';

describe('IncomeToggleCasePipe', () => {
  it('create an instance', () => {
    const pipe = new IncomeToggleCasePipe();
    expect(pipe).toBeTruthy();
  });
});
